name: cicd-manual

on:
  workflow_dispatch: 

jobs:
# every job needs runner 
  Code:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout code
        run: echo "code Clone ho gaya"
  Build:
    needs: [Code]
    runs-on: ubuntu-latest
    steps:
     - name: Build code
       run: echo "code Build ho gaya"
  Deploy:
    needs: [Code, Build]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy code
        run: echo "code Deploy ho gaya"

  show_versions:
    runs-on: ubuntu-latest
    steps:
     - name: Checks Docker & Python Version
       run: docker --version & python3 --version

    
    
