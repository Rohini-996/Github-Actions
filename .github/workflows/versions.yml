name: Prints versions

on:
  workflow_dispath:

    jobs:

      check_versions:
        runs-on: ubuntu-latest
        steps:
          - name: Checking Docker & Python Version
            run: docker --version & python3 --version




          

